const x = "Voyager!";

const hello = () => {
  console.log(`Hello, ${x}!`);
};

hello();

document.getElementById("list").style.height = (window.innerHeight - 200) + "px";
// console.log(window.innerHeight);
